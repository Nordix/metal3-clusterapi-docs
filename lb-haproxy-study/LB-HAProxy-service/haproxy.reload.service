[Unit]
Description=Monitor the file stopping file for changes, touch is enough

[Path]
PathChanged=/haproxy/haproxy.cfg
Unit=haproxy.reload.service

[Install]
WantedBy=multi-user.target
[vagrant@master1 system]$ cat haproxy.reload.service
[Unit]
Description=HAProxy service reloader

[Service]
ExecStart=/bin/systemctl reload haproxy

[Install]
WantedBy=multi-user.target

